{
  "private": true,
  "workspaces": [
    "functions",
    "web"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -Ap run build",
    "lint": "run-p 'lint:*'",
    "lint:oxlint": "oxlint --type-aware && yarn workspaces foreach -Ap run lint",
    "lint:fmt": "oxfmt --check",
    "fmt": "oxfmt"
  },
  "dependencies": {
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "oxlint-tsgolint": "^0.11.5"
  },
  "engines": {
    "node": ">=24",
    "yarn": ">=4.12.0"
  },
  "packageManager": "yarn@4.12.0"
}
