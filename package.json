{
  "workspaces": [
    "functions",
    "web"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -Ap run build",
    "lint": "oxlint",
    "fmt": "oxfmt"
  },
  "dependencies": {
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0"
  },
  "engines": {
    "node": ">=24",
    "yarn": ">=4.12.0"
  },
  "packageManager": "yarn@4.12.0"
}
